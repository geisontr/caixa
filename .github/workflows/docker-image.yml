name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    
    - name: Build the Docker image
      uses: docker/login-action@v3    
      
    - name: Login no Docker Hub
      uses: docker/login-action@v3
      with:
       username: dhgtr
       password: git_token
    
    - name: Build e Push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: dhgtr/repo01:latest
      

